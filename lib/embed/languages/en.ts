import { EMBED_STRINGS } from "@/packages/shared/embed-strings";

import type { EmbedLanguage } from "./types";

export const EN: EmbedLanguage = {
  id: "en",
  label: "English",
  strings: EMBED_STRINGS,
  badge: "Made with {brand}",
};
